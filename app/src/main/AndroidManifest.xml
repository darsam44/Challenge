<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.challenge">
    <!-- //Adding  Read External Storge Premission -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- //Give premision to connect to the INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--Premission -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Challenge">
        <activity android:name=".Acvtivities.AddVido"></activity>
        <activity android:name=".Acvtivities.VideoAct" />
        <activity android:name=".Acvtivities.Admin" />
        <activity android:name=".Acvtivities.Family" />
        <activity android:name=".Acvtivities.Scary" />
        <activity android:name=".Acvtivities.Funny" />
        <activity android:name=".Acvtivities.Profile" />
        <activity android:name=".Acvtivities.new_challenge" />
        <activity android:name=".Acvtivities.Main_Page" />
        <activity android:name=".Acvtivities.Register" />
        <activity android:name=".Acvtivities.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>